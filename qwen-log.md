# First read `AGENTS.md`. After reading `AGENTS.md` read the rest of `qwen-log.md`.
# You task is to create an Object-Oriented Programing class diagram of `mirror-remote-directory.sh`.
# You cannot read `mirror-remote-directory.sh` as it is too large for your context window.
# Read any steps you have logged to `qwen-log.md` first.
# Then if you haven't already, start by reading the functions names in `mirror-remote-directory.sh`.
# Once you have the names save them to `class-diagram.md`. Then continue examining the file.
# REMEMBER: Save every step to `qwen-log.md`.
# REMEMBER: Any time you discover anything that should be added to `class-diagram.md`, you must save that information before doing ANYTHING else.
